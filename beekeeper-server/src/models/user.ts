class User {
    name: string;
    email: string;
    password: string;
    
    constructor(name, email, password) {
       this.email = email;
       this.password = password;
       this.name = name;
    }
}